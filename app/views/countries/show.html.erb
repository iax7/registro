<p id="notice"><%= notice %></p>

<div class="row">
  <div class="col-sm-2"><strong><%= label :country, :name %>:</strong></div>
  <div class='col-sm-10'><%= @country.name %></div>
</div>
<div class="row">
  <div class="col-sm-2"><strong><%= label :country, :iso %>:</strong></div>
  <div class='col-sm-10'><%= @country.iso %></div>
</div>

<%= link_to 'Edit', edit_country_path(@country), { class: 'btn btn-xs btn-default' } %> |
<%= link_to 'Back', countries_path, { class: 'btn btn-xs btn-default' } %>
